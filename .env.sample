# Copy this file to `.env` and fill in values as needed.
# NOTE: These services do not automatically load `.env` at runtime.
# - If you use VS Code debugging, add "envFile": "${workspaceFolder}/.env" to your launch config.
# - Or export env vars in your shell before running.


# ------------------------------
# Azure AI Foundry (Agent Framework)
# ------------------------------
# Required for both writer and reviewer:
AZURE_AI_PROJECT_ENDPOINT="https://<your-foundry-project-endpoint>"
AZURE_AI_MODEL_DEPLOYMENT_NAME="<your-model-deployment-name>"

# Optional: reuse an existing persistent agent
# AZURE_AI_AGENT_ID="<existing-agent-id>"


# ------------------------------
# Azure Identity (DefaultAzureCredential)
# ------------------------------
# For local development, DefaultAzureCredential can use Azure CLI login, VS Code login,
# or environment-based credentials. If you want env-based auth, set:
# AZURE_TENANT_ID="<tenant-guid>"
# AZURE_CLIENT_ID="<app-registration-client-id>"
# AZURE_CLIENT_SECRET="<client-secret>"
# 
# If you use Managed Identity in Azure, you typically do NOT set secrets.


# ------------------------------
# A2A (Agent-to-Agent) card URL
# ------------------------------
# Used to populate the agent card `url` as: ${A2A_PUBLIC_URL}/a2a
# Set this per service when running them separately.
# Writer (default port 8000):
# A2A_PUBLIC_URL="http://localhost:8000"
# Reviewer (default port 8001):
# A2A_PUBLIC_URL="http://localhost:8001"


# ------------------------------
# Service metadata
# ------------------------------
# Used for telemetry service.name and /healthz responses.
# SERVICE_NAME="writer-agent"   # or reviewer-agent

# Optional overrides for display/agent card metadata:
# WRITER_AGENT_NAME="writer-agent"
# WRITER_AGENT_DESCRIPTION="Writes a short summary for a user-provided topic."
# REVIEWER_AGENT_NAME="reviewer-agent"
# REVIEWER_AGENT_DESCRIPTION="Reviews and improves a writer draft for a given topic."


# ------------------------------
# Model call tuning (shared)
# ------------------------------
# These apply to BOTH services if set.
# AGENT_TIMEOUT_SECONDS="30"
# AGENT_MAX_TOKENS="400"         # reviewer default is 500
# AGENT_TEMPERATURE="0.3"        # reviewer default is 0.2
